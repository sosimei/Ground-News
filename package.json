{
  "name": "ground-news",
  "version": "1.0.0",
  "description": "Ground News Application",
  "dependencies": {
    "serverless-http": "^3.2.0",
    "express": "^4.18.2",
    "mongodb": "^5.7.0"
  },
  "scripts": {
    "start": "cd client && npm start",
    "build": "cd client && npm run build"
  }
}